---
description: Show the command that opens your team console in the browser
disable-model-invocation: true
---

Show me, in one code block I can copy, the command that starts the team relay's local console in a
terminal. Do not run it yourself, and do not change it:

```
"${CLAUDE_PLUGIN_ROOT}/bin/console" --open
```

Then tell me, briefly: it uses the same sign-in as `/team-relay:login`; it listens on 127.0.0.1 only and
opens a link with a one-time key in my browser; it shows the team map, the activity and, for team
owners, the Members panel; Ctrl-C stops it.
